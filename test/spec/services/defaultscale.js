'use strict';

describe('Service: DefaultScale', function() {

    // load the service's module
    beforeEach(module('chesireApp'));

    // instantiate service
    var DefaultScale;
    beforeEach(inject(function(_DefaultScale_) {
        DefaultScale = _DefaultScale_;
    }));

    // it('should do something', function() {
    //     expect( !! DefaultScale).toBe(true);
    // });

});