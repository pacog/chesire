'use strict';

describe('Service: OscillatorCollection', function() {

    // load the service's module
    beforeEach(module('chesireApp'));

    // instantiate service
    var OscillatorCollection;
    beforeEach(inject(function(_OscillatorCollection_) {
        OscillatorCollection = _OscillatorCollection_;
    }));

    // it('should do something', function() {
    //     expect( !! OscillatorCollection).toBe(true);
    // });

});