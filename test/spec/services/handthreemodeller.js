'use strict';

describe('Service: Handthreemodeller', function() {

    // load the service's module
    beforeEach(module('chesireApp'));

    // instantiate service
    var Handthreemodeller;
    beforeEach(inject(function(_Handthreemodeller_) {
        Handthreemodeller = _Handthreemodeller_;
    }));

    // it('should do something', function() {
    //     expect( !! Handthreemodeller).toBe(true);
    // });

});